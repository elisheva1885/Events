// 🔹 יצירת JWT
function generateToken(payload) {}

// 🔹 אימות JWT
function verifyToken(token) {}

// 🔹 חידוש JWT
function refreshToken(token) {}

module.exports = {
    generateToken,
    verifyToken,
    refreshToken
};
