// 🔹 שליפת התראות למשתמש
async function getNotifications(userId) {}

// 🔹 שליחת התראה לדוגמה / בדיקה
async function sendTestNotification(userId, notificationData) {}

module.exports = {
    getNotifications,
    sendTestNotification
};